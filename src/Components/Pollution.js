import { useLocation } from "react-router-dom";



const Pollution = () => {
    let location = useLocation();
return(
    <div>
        This is pollution page
    </div>
)
}

export default Pollution;